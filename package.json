{
  "scripts": {
    "build": "vite build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "three": "^0.171.0"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0",
    "vite": "^6.0.6"
  }
}